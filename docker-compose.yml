services:
  backend:
    build:
      context: .
      dockerfile: backend.Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./model/weights:/app/model/weights
    environment:
      - PYTHONUNBUFFERED=1
      - PORT=8000
      - ALLOWED_ORIGINS=http://localhost:5173
