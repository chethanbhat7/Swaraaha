# Backend API base URL (leave blank to use relative /api in dev).
# Origin (https://api.example.com) or origin with /api both work;
# trailing slashes are normalized automatically.
VITE_API_BASE_URL=
