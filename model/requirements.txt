# Swaraaha - Model Dependencies
# Install: pip install -r requirements.txt

# Core ML
torch>=2.0.0                # PyTorch deep learning framework (use --index-url .../cpu for CPU-only)
numpy>=1.24.0               # Numerical computing, array operations

# Audio Processing
librosa>=0.10.0             # Audio analysis, mel-spectrogram generation
soundfile>=0.12.0           # Reading/writing audio files (.wav, .flac, etc.)
audioread>=3.0.0            # Backend for librosa audio I/O (installed with librosa)

# Scientific Computing
scipy>=1.10.0               # Signal processing, scientific algorithms

# Visualization
matplotlib>=3.7.0           # Plotting spectrograms, training curves, debug output

# Numba (installed with librosa, used for JIT compilation)
numba>=0.57.0               # JIT compiler for numerical functions (librosa dependency)

# Hugging Face
transformers>=4.30.0       # Wav2Vec 2.0 models

# Dataset Download
kaggle>=1.5.16             # Kaggle dataset API (for SEP-28K, UCLASS)

# Environment
python-dotenv>=1.0.0       # Load .env credentials for Kaggle downloads
